using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class unparentChestObject : MonoBehaviour {


	/*
	public GameObject chest;


	void Start() {
		chest = gameObject.transform.Find("TreasureChest");
	}

	void OnCollisionEnter(Collision col) {
		if (col.gameObject.tag == "Grabbable") {
			if (col.gameObject.transform.parent != null) {	
				
			}
			else {
				col.transform.SetParent(chest.transform);
			}
			//col.transform.position = chest.transform.position;
		}
	}


	void OnCollisionExit(Collision other) {
		if (other.gameObject.tag == "Grabbable") {
			other.gameObject.transform.parent = null;
		}


	}	*/
}
